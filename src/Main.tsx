import React from 'react';
import {SafeAreaView, Text} from 'react-native';

export default function Main(): JSX.Element {
  return (
    <SafeAreaView>
      <Text>We at the main screen :)</Text>
    </SafeAreaView>
  );
}
